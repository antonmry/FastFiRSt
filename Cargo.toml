[workspace]
members = [
    "flash-lib",
    "flash-cli",
    "flash-df",
    "flash-wasm",
    "fastq-gen-cli",
]
resolver = "2"

[workspace.dependencies]
anyhow = "1"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[patch.crates-io]
flash-lib = { path = "flash-lib" }
