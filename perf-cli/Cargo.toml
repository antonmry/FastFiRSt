[package]
name = "perf-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
clap = { version = "4.5", features = ["derive"] }
perf-lib = { path = "../perf-lib", features = ["parallel"] }
