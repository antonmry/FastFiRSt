[package]
name = "perf-lib"
version = "0.1.0"
edition = "2024"

[features]
parallel = ["rayon"]

[dependencies]
anyhow.workspace = true
flate2 = "1"
rayon = { version = "1.10", optional = true }

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
